<!DOCTYPE html>
<html lang="en">
    <%- include(`components/heading.ejs`, {title: 'Add Player'}) %>
<body>

    <div class="container">
        <h1 class="text-center text-muted">Add Player</h1>

        <div class="d-flex justify-content-center">
            <form action="/players/add" method="post">
                <p>Name</p>
                <input type="text" name="name">
                <p>Age</p>
                <input type="text" name="age">
                <p>Position</p>
                <select name="position">
                    <option selected disabled>--Position--</option>
                    <% ["Striker", "Midfielder", "Defender", "Goalkeeper"].forEach(el => { %>
                        <option value="<%= el %>"><%= el %></option>
                    <% }) %>
                </select>
                <p>Natonality</p>
                <select name="nationality">
                    <option selected disabled>--Nationality--</option>
                    <% ["Brazil", "England", "France", "Portugal", "Spain"].forEach(el => { %>
                        <option value="<%= el %>"><%= el %></option>
                    <% }) %>
                </select>
                <p>Rating</p>
                <input type="number" name="rating">
                <p>Club</p>
                <select name="ClubId">
                    <option selected disabled>--Club--</option>
                    <% data.forEach(el => { %>
                        <option value="<%= el.id %>"><%= el.name %></option>
                    <% }) %>
                </select>
                <p></p>
                <button class="btn btn-primary" type="submit">Submit</button>
            </form>
        </div>
    
    </div>
</body>
</html>